import { ITransferInToExDetails } from "../transferTypes";

export default class TransferInToExDetail implements TransferInToExDetail {
  async getDetails(): Promise<any> {
    try {
      return {};
    } 
  }
}
